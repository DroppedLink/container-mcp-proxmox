# Proxmox MCP Server Configuration
# Copy this file to .env and update with your actual values

# Proxmox VE API Configuration
PROXMOX_HOST=your-proxmox-host.example.com
PROXMOX_USER=your-username@pam
PROXMOX_PASSWORD=your-password
PROXMOX_VERIFY_SSL=false

# Optional: Server Configuration (for running the server)
# MCP_HOST=0.0.0.0
# MCP_PORT=8001

# For testing scripts - specify where your MCP server is running
# MCP_SERVER_HOST=localhost
# MCP_SERVER_PORT=8001

# Proxmox Infrastructure Defaults (optional overrides)
# PROXMOX_DEFAULT_STORAGE=local-lvm
# PROXMOX_DEFAULT_BRIDGE=vmbr0

# Test Configuration (optional overrides)
# PROXMOX_TEST_VMID_START=9990
# PROXMOX_TEST_VMID_END=9999
# PROXMOX_TEST_PASSWORD_PREFIX=MCPTest 