# Connection URL for the NATS server
NATS_URL=nats://localhost:4222
# Example for Docker Compose: NATS_URL=nats://nats:4222

# Proxmox API Connection Details
PROXMOX_HOST=pve.example.com
PROXMOX_USER=root@pam # Use the Proxmox user, e.g., root@pam or an API token user like user@pve!tokenid
PROXMOX_PASSWORD=YOUR_PROXMOX_PASSWORD_OR_API_TOKEN_SECRET # Use the user's password or the API token secret
PROXMOX_VERIFY_SSL=false # Set to true in production with valid SSL certificates for Proxmox VE

# Name of this service for discovery and NATS subject construction
MCP_SERVICE_NAME=proxmox

# Optional: FastAPI/Uvicorn settings for running locally (if not using Docker CMD/ENTRYPOINT)
# HOST=0.0.0.0
# PORT=8000